services:
  alert-enricher:
    build: .
    container_name: hermes-alert-enricher
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - ./config/enricher.env
    networks:
      - hermes-net

networks:
  hermes-net:
    external: true
